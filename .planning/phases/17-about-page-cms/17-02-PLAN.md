---
phase: 17-about-page-cms
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified: [src/components/admin/AboutEditor.jsx, src/pages/AdminPage.jsx, src/pages/AboutPage.jsx]
autonomous: true
---

<objective>
Create admin UI for editing About page content and update frontend to fetch from database.

Purpose: Complete the CMS integration so admin edits appear on the public About page.
Output: AboutEditor component in admin panel, AboutPage fetches from Supabase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-about-page-cms/17-01-PLAN.md

@src/pages/AdminPage.jsx
@src/pages/AboutPage.jsx
@src/components/admin/PhotoManager.jsx
@src/services/aboutService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutEditor admin component</name>
  <files>src/components/admin/AboutEditor.jsx</files>
  <action>
Create AboutEditor following existing admin component patterns:

```jsx
import React, { useState, useEffect } from 'react';
import { Save } from 'lucide-react';
import { fetchAboutContent, updateAboutContent } from '../../services/aboutService';

function AboutEditor() {
  const [content, setContent] = useState(null);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState(null);
  const [success, setSuccess] = useState(false);

  useEffect(() => {
    async function loadContent() {
      setLoading(true);
      const data = await fetchAboutContent();
      if (data) {
        setContent(data);
      } else {
        setError('Failed to load content');
      }
      setLoading(false);
    }
    loadContent();
  }, []);

  async function handleSave() {
    setSaving(true);
    setError(null);
    setSuccess(false);

    const result = await updateAboutContent({
      youtube_video_id: content.youtube_video_id,
      bio_paragraph_1: content.bio_paragraph_1,
      bio_paragraph_2: content.bio_paragraph_2,
      bio_paragraph_3: content.bio_paragraph_3,
      youtube_url: content.youtube_url,
      instagram_url: content.instagram_url
    });

    if (result.success) {
      setSuccess(true);
      setTimeout(() => setSuccess(false), 3000);
    } else {
      setError(result.error || 'Failed to save');
    }

    setSaving(false);
  }

  function handleChange(field, value) {
    setContent(prev => ({ ...prev, [field]: value }));
  }

  if (loading) {
    return <div className="text-center py-8 text-gray-500">Loading content...</div>;
  }

  if (!content) {
    return <div className="text-center py-8 text-red-500">{error || 'Failed to load'}</div>;
  }

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h2 className="text-xl font-semibold text-gray-800">About Page Editor</h2>
        <button
          onClick={handleSave}
          disabled={saving}
          className={`flex items-center gap-2 px-4 py-2 bg-primary text-white rounded-lg transition-colors ${saving ? 'opacity-50 cursor-not-allowed' : 'hover:bg-primary/90'}`}
        >
          <Save size={18} />
          {saving ? 'Saving...' : 'Save Changes'}
        </button>
      </div>

      {error && (
        <div className="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm">
          {error}
        </div>
      )}

      {success && (
        <div className="mb-4 p-3 bg-green-50 border border-green-200 rounded-lg text-green-700 text-sm">
          Changes saved successfully!
        </div>
      )}

      <div className="space-y-6">
        {/* YouTube Video ID */}
        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            YouTube Video ID
          </label>
          <input
            type="text"
            value={content.youtube_video_id || ''}
            onChange={(e) => handleChange('youtube_video_id', e.target.value)}
            placeholder="e.g., dQw4w9WgXcQ"
            className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary"
          />
          <p className="mt-1 text-sm text-gray-500">
            The ID from the YouTube URL (e.g., youtube.com/watch?v=<strong>dQw4w9WgXcQ</strong>)
          </p>
        </div>

        {/* Bio Paragraphs */}
        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            Bio - Paragraph 1
          </label>
          <textarea
            value={content.bio_paragraph_1 || ''}
            onChange={(e) => handleChange('bio_paragraph_1', e.target.value)}
            rows={3}
            className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary resize-y"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            Bio - Paragraph 2
          </label>
          <textarea
            value={content.bio_paragraph_2 || ''}
            onChange={(e) => handleChange('bio_paragraph_2', e.target.value)}
            rows={3}
            className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary resize-y"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            Bio - Paragraph 3
          </label>
          <textarea
            value={content.bio_paragraph_3 || ''}
            onChange={(e) => handleChange('bio_paragraph_3', e.target.value)}
            rows={3}
            className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary resize-y"
          />
        </div>

        {/* Social Links */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label className="block text-sm font-medium text-gray-700 mb-2">
              YouTube Channel URL
            </label>
            <input
              type="url"
              value={content.youtube_url || ''}
              onChange={(e) => handleChange('youtube_url', e.target.value)}
              placeholder="https://youtube.com/@yourchannel"
              className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary"
            />
          </div>

          <div>
            <label className="block text-sm font-medium text-gray-700 mb-2">
              Instagram URL
            </label>
            <input
              type="url"
              value={content.instagram_url || ''}
              onChange={(e) => handleChange('instagram_url', e.target.value)}
              placeholder="https://instagram.com/yourhandle"
              className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary"
            />
          </div>
        </div>
      </div>

      <p className="mt-6 text-sm text-gray-500">
        Last updated: {content.updated_at ? new Date(content.updated_at).toLocaleString() : 'Never'}
      </p>
    </div>
  );
}

export default AboutEditor;
```

Key features:
- Form with all editable fields
- Save button with loading state
- Success/error feedback
- Last updated timestamp display
  </action>
  <verify>npm run build succeeds</verify>
  <done>AboutEditor component created with form for all About page fields</done>
</task>

<task type="auto">
  <name>Task 2: Add About tab to AdminPage</name>
  <files>src/pages/AdminPage.jsx</files>
  <action>
1. Import AboutEditor:
   ```jsx
   import AboutEditor from '../components/admin/AboutEditor';
   ```

2. Add 'about' to tabs array:
   ```jsx
   const tabs = [
     { id: 'dashboard', label: 'Dashboard' },
     { id: 'locations', label: 'Locations' },
     { id: 'suggestions', label: 'Suggestions' },
     { id: 'reminders', label: 'Reminders' },
     { id: 'videos', label: 'Videos' },
     { id: 'photos', label: 'Photos' },
     { id: 'about', label: 'About' }
   ];
   ```

3. Add About tab content after Photos tab:
   ```jsx
   {/* About Tab */}
   {activeTab === 'about' && <AboutEditor />}
   ```
  </action>
  <verify>npm run build succeeds, About tab appears in admin navigation</verify>
  <done>About tab added to AdminPage with AboutEditor component</done>
</task>

<task type="auto">
  <name>Task 3: Update AboutPage to fetch from database</name>
  <files>src/pages/AboutPage.jsx</files>
  <action>
Update AboutPage to fetch content from database with loading/error states:

1. Add imports:
   ```jsx
   import React, { useState, useEffect } from 'react';
   import { fetchAboutContent } from '../services/aboutService';
   ```

2. Add state and useEffect for data fetching:
   ```jsx
   const [content, setContent] = useState(null);
   const [loading, setLoading] = useState(true);

   useEffect(() => {
     async function loadContent() {
       const data = await fetchAboutContent();
       if (data) {
         setContent(data);
       }
       setLoading(false);
     }
     loadContent();
   }, []);
   ```

3. Add loading state before main content:
   ```jsx
   if (loading) {
     return (
       <div className="flex-1 flex items-center justify-center bg-background">
         <p className="text-gray-500">Loading...</p>
       </div>
     );
   }
   ```

4. Replace hardcoded values with database content:
   - YouTube embed: `src="https://www.youtube.com/embed/${content?.youtube_video_id || 'YOUR_VIDEO_ID'}"`
   - Bio paragraphs: `{content?.bio_paragraph_1 || 'Default text...'}` (with fallbacks)
   - YouTube link: `href={content?.youtube_url || 'https://youtube.com/@yourchannel'}`
   - Instagram link: `href={content?.instagram_url || 'https://instagram.com/capt_gr8_1'}`

Keep the stats section (Locations, Countries, Videos) unchanged - that's Phase 18.
  </action>
  <verify>npm run build succeeds, AboutPage renders with database content or fallbacks</verify>
  <done>AboutPage fetches content from database with loading state and fallbacks</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes (or only pre-existing errors)
- [ ] AboutEditor component exists with form fields
- [ ] AdminPage has About tab in navigation
- [ ] AboutPage fetches from database
</verification>

<success_criteria>
- All tasks completed
- AboutEditor follows existing admin component patterns
- About tab integrated into AdminPage
- AboutPage displays database content with fallbacks
</success_criteria>

<output>
After completion, create `.planning/phases/17-about-page-cms/17-02-SUMMARY.md`
</output>
