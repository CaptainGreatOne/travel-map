---
phase: 16-photography-page-cms
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified: [src/pages/PhotographyPage.jsx, src/data/sampleData.js]
autonomous: true
---

<objective>
Update Photography page to fetch photos from database and remove sample data dependency.

Purpose: Complete the CMS integration so admin-uploaded photos appear on the public page.
Output: PhotographyPage fetches from Supabase, sample photo data removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-photography-page-cms/16-CONTEXT.md
@.planning/phases/16-photography-page-cms/16-01-SUMMARY.md
@.planning/phases/16-photography-page-cms/16-02-SUMMARY.md

@src/pages/PhotographyPage.jsx
@src/data/sampleData.js
@src/services/photoService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PhotographyPage to fetch from database</name>
  <files>src/pages/PhotographyPage.jsx</files>
  <action>
Replace static import with database fetch:

```jsx
import React, { useState, useEffect } from 'react';
import PhotoCarousel from '../components/PhotoCarousel';
import { fetchPhotos } from '../services/photoService';

function PhotographyPage() {
  const [photos, setPhotos] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    async function loadPhotos() {
      setLoading(true);
      const data = await fetchPhotos();

      if (data) {
        setPhotos(data);
        setError(null);
      } else {
        setError('Unable to load photos. Please try again later.');
      }

      setLoading(false);
    }

    loadPhotos();
  }, []);

  return (
    <div className="flex-1 overflow-y-auto bg-background">
      {/* Page Header */}
      <div className="py-8 md:py-12 px-4 md:px-16 pb-6 md:pb-8 bg-gradient-to-br from-secondary to-secondary/80 text-white">
        <h1 className="m-0 mb-2 text-2xl md:text-4xl font-bold">
          Photography
        </h1>
        <p className="m-0 text-base md:text-lg opacity-90 font-light">
          Capturing moments from around the world
        </p>
      </div>

      {/* Content Container */}
      <div className="max-w-[1400px] mx-auto py-6 md:py-12 px-4 md:px-16">

        {/* Featured Carousel */}
        <div className="mb-8 md:mb-16">
          <h2 className="m-0 mb-4 md:mb-6 text-2xl md:text-3xl text-secondary font-semibold">
            Featured Photos
          </h2>

          {loading ? (
            <div className="flex items-center justify-center py-16 bg-gray-50 rounded-xl">
              <p className="text-gray-500">Loading photos...</p>
            </div>
          ) : error ? (
            <div className="flex items-center justify-center py-16 bg-red-50 rounded-xl border border-red-200">
              <p className="text-red-600">{error}</p>
            </div>
          ) : photos.length === 0 ? (
            <div className="flex items-center justify-center py-16 bg-gray-50 rounded-xl">
              <p className="text-gray-500">No photos available yet.</p>
            </div>
          ) : (
            <PhotoCarousel photos={photos} />
          )}
        </div>

        {/* Instagram Section */}
        <div className="text-center py-8 md:py-12 px-4 md:px-8 bg-white rounded-xl shadow-md">
          <h2 className="m-0 mb-3 md:mb-4 text-2xl md:text-3xl text-secondary font-semibold">
            Follow My Journey
          </h2>
          <p className="m-0 mb-6 md:mb-8 text-base md:text-lg text-gray-600">
            See more photos and daily updates on Instagram
          </p>
          <a
            href="https://instagram.com/capt_gr8_1"
            target="_blank"
            rel="noopener noreferrer"
            className="inline-block w-full sm:w-auto px-6 md:px-8 py-3 md:py-4 bg-pink-600 text-white no-underline rounded-lg text-base md:text-lg font-semibold shadow-lg shadow-pink-600/30 hover:shadow-xl hover:shadow-pink-600/40 hover:-translate-y-0.5 transition-all duration-200"
          >
            📸 @yourhandle
          </a>
        </div>
      </div>
    </div>
  );
}

export default PhotographyPage;
```

Key changes:
- Add useState for photos, loading, error
- Add useEffect to fetch photos on mount
- Import fetchPhotos from photoService
- Remove import from sampleData
- Show loading state while fetching
- Show error message if fetch fails
- Show "No photos" message if empty array
- Pass fetched photos to PhotoCarousel
  </action>
  <verify>npm run build succeeds</verify>
  <done>PhotographyPage fetches photos from database with proper loading/error states</done>
</task>

<task type="auto">
  <name>Task 2: Remove featuredPhotos from sampleData.js</name>
  <files>src/data/sampleData.js</files>
  <action>
Remove the featuredPhotos export from sampleData.js:

1. Delete the entire `export const featuredPhotos = [...]` array (lines ~163-194)
2. Keep the categories and sampleLocations exports (they may still be used as fallback elsewhere)

The file should end after sampleLocations without the featuredPhotos export.
  </action>
  <verify>npm run build succeeds, no import errors for featuredPhotos</verify>
  <done>featuredPhotos removed from sampleData.js</done>
</task>

<task type="auto">
  <name>Task 3: Verify PhotoCarousel compatibility</name>
  <files>src/components/PhotoCarousel.jsx</files>
  <action>
Read PhotoCarousel to ensure it's compatible with the database photo schema.

Database photos have: { id, url, title, location, ... }
Sample photos had: { id, url, title, location }

If PhotoCarousel expects the same shape, no changes needed.
If there are mismatches, update PhotoCarousel to use the correct field names.

Expected: No changes needed since schema matches.
  </action>
  <verify>PhotoCarousel renders with database photo objects</verify>
  <done>PhotoCarousel compatible with database photo schema</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] PhotographyPage shows loading state initially
- [ ] PhotographyPage shows error if database unavailable
- [ ] featuredPhotos removed from sampleData.js
- [ ] No broken imports for featuredPhotos
</verification>

<success_criteria>
- All tasks completed
- Photography page fetches from database
- Loading and error states display correctly
- Sample photo data removed
</success_criteria>

<output>
After completion, create `.planning/phases/16-photography-page-cms/16-03-SUMMARY.md`
</output>
