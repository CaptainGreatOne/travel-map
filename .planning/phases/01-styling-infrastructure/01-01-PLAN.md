---
phase: 01-styling-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tailwind.config.js, postcss.config.js, src/index.css, src/App.css]
autonomous: true
---

<objective>
Install and configure Tailwind CSS with project-specific design tokens.

Purpose: Establish CSS foundation that all subsequent component conversions will use.
Output: Working Tailwind setup with custom colors matching existing design.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/index.css
@src/App.css
@src/components/Sidebar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind CSS and dependencies</name>
  <files>package.json</files>
  <action>
    Install Tailwind CSS v3 with PostCSS and Autoprefixer:
    ```
    npm install -D tailwindcss postcss autoprefixer
    npx tailwindcss init -p
    ```
    This creates tailwind.config.js and postcss.config.js.
  </action>
  <verify>npm ls tailwindcss shows installed version; tailwind.config.js exists</verify>
  <done>Tailwind CSS installed as devDependency</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind with project design tokens</name>
  <files>tailwind.config.js, src/index.css</files>
  <action>
    Configure tailwind.config.js:
    - Set content paths: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
    - Extend theme with custom colors extracted from codebase:
      - primary: '#f4a261' (accent/brand orange)
      - secondary: '#2d4356' (dark blue-gray sidebar)
      - muted: '#b8c5d6' (light gray text)
      - background: '#f5f5f5' (light background)
    - Add custom spacing if needed

    Update src/index.css:
    - Add Tailwind directives at top:
      @tailwind base;
      @tailwind components;
      @tailwind utilities;
    - Keep any legitimate global styles (body resets, font imports)
  </action>
  <verify>npm run dev starts without PostCSS errors</verify>
  <done>Tailwind configured with project colors; directives in index.css</done>
</task>

<task type="auto">
  <name>Task 3: Clean up unused Vite boilerplate CSS</name>
  <files>src/App.css</files>
  <action>
    Remove or replace src/App.css content:
    - Current content is Vite starter template (logo animations, .card class)
    - None of this is used in the actual app
    - Either delete file entirely OR replace with:
      /* App-level styles - use Tailwind utilities in components */

    Verify App.jsx imports - remove App.css import if file deleted.

    DO NOT delete index.css - it now has Tailwind directives.
  </action>
  <verify>npm run build succeeds; no CSS-related warnings</verify>
  <done>Unused boilerplate removed; clean CSS foundation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] Tailwind classes work (test by adding `className="text-red-500"` to any element)
- [ ] Custom colors available (test with `className="text-primary"`)
</verification>

<success_criteria>
- Tailwind CSS installed and configured
- Custom colors defined matching existing design
- Vite boilerplate CSS removed
- App still runs and looks the same (no visual regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-styling-infrastructure/01-01-SUMMARY.md`
</output>
