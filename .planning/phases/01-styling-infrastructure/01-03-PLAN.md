---
phase: 01-styling-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/components/AuthModal.jsx, src/components/LocationSuggestionForm.jsx]
autonomous: true
---

<objective>
Convert modal and form components from inline styles to Tailwind CSS.

Purpose: Migrate authentication and form UI to Tailwind with proper form styling patterns.
Output: AuthModal and LocationSuggestionForm using Tailwind classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-styling-infrastructure/01-01-SUMMARY.md

@src/components/AuthModal.jsx
@src/components/LocationSuggestionForm.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert AuthModal.jsx to Tailwind</name>
  <files>src/components/AuthModal.jsx</files>
  <action>
    Replace all inline style={{...}} with Tailwind classes.

    Modal patterns:
    - Overlay: fixed inset-0 bg-black/50 flex items-center justify-center z-50
    - Modal box: bg-white rounded-xl p-6 w-full max-w-md shadow-xl
    - Close button: absolute top-4 right-4 text-gray-400 hover:text-gray-600
    - Form inputs: w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent
    - Submit button: w-full bg-primary text-white py-2 rounded-lg hover:bg-primary/90 transition-colors
    - Error message: text-red-500 text-sm mt-1

    Handle conditional rendering (isOpen check) - keep early return pattern.

    Remove onMouseEnter/onMouseLeave hover handlers - use hover: classes.
  </action>
  <verify>npm run dev - click Sign In, modal opens with correct styling</verify>
  <done>AuthModal.jsx converted to Tailwind; modal overlay and form styled</done>
</task>

<task type="auto">
  <name>Task 2: Convert LocationSuggestionForm.jsx to Tailwind</name>
  <files>src/components/LocationSuggestionForm.jsx</files>
  <action>
    Replace all inline style={{...}} with Tailwind classes.

    Form patterns:
    - Form container: space-y-4 (gap between form fields)
    - Labels: block text-sm font-medium text-gray-700 mb-1
    - Inputs: w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent
    - Textarea: Same as input but with resize-none min-h-[100px]
    - Submit button: w-full bg-primary text-white py-3 rounded-lg font-medium hover:bg-primary/90 disabled:opacity-50 disabled:cursor-not-allowed
    - Success message: bg-green-50 border border-green-200 text-green-700 p-4 rounded-lg
    - Error message: bg-red-50 border border-red-200 text-red-700 p-4 rounded-lg

    Preserve form validation and submission logic.
  </action>
  <verify>npm run dev - navigate to Suggest, form displays correctly</verify>
  <done>LocationSuggestionForm.jsx converted to Tailwind; form elements styled</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] AuthModal opens/closes correctly
- [ ] Modal overlay dims background
- [ ] Form inputs have focus states
- [ ] Submit buttons have hover states
- [ ] LocationSuggestionForm displays correctly
- [ ] No style={{}} remains in converted files
</verification>

<success_criteria>
- Both components converted to Tailwind
- Modal overlay and positioning work correctly
- Form inputs have proper focus states
- Buttons have hover and disabled states
- No inline style objects remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-styling-infrastructure/01-03-SUMMARY.md`
</output>
